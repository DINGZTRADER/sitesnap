# These browser-safe settings belong in the local/Vercel environment only.
# Cloud mode is selected only when both values are present.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Supabase Auth callback to allow for the pilot domain:
# https://sitesnap.wachaai.com/auth/callback
