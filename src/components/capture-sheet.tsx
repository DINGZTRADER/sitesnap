'use client';
import { useState } from 'react'; import { Check, ImagePlus, X } from 'lucide-react'; import { Tag } from '@/types/domain';
const availableTags:Tag[]=['Daily Progress','Pre-Cover','Firestop Inspection','JCT Variation','Snagging Defect'];
export function CaptureSheet({onClose,onSaved}:{onClose:()=>void;onSaved:()=>void}){const [tags,setTags]=useState<Tag[]>(['Daily Progress']);const [note,setNote]=useState('');const [file,setFile]=useState<File|null>(null);return <div className="fixed inset-0 z-50 flex items-end justify-center bg-ink/50 p-0 backdrop-blur-sm sm:items-center sm:p-5"><div className="w-full max-w-lg rounded-t-3xl bg-white p-5 sm:rounded-3xl"><div className="flex items-start justify-between"><div><p className="text-xs font-black uppercase tracking-wider text-ink/45">New evidence record</p><h2 className="mt-1 text-2xl font-black">Add a site photo</h2></div><button onClick={onClose} aria-label="Close" className="rounded-full bg-ink/5 p-2"><X size={18}/></button></div><label className="mt-5 flex cursor-pointer flex-col items-center justify-center rounded-2xl border-2 border-dashed border-ink/15 bg-[#f6f8f5] px-4 py-8 text-center"><ImagePlus size={28} className="text-ink/40"/><span className="mt-2 text-sm font-black">{file?file.name:'Take or choose a photo'}</span><span className="mt-1 text-xs text-ink/45">Timestamp and operator are added automatically</span><input className="sr-only" type="file" accept="image/*" capture="environment" onChange={e=>setFile(e.target.files?.[0]??null)}/></label><label className="mt-4 block text-xs font-black uppercase tracking-wider text-ink/50">Site note<textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="What should the team know?" className="mt-2 min-h-20 w-full resize-none rounded-xl border border-ink/10 p-3 text-sm outline-none focus:border-ink"/></label><div className="mt-4"><p className="text-xs font-black uppercase tracking-wider text-ink/50">Tags</p><div className="mt-2 flex flex-wrap gap-2">{availableTags.map(tag=><button type="button" key={tag} onClick={()=>setTags(t=>t.includes(tag)?t.filter(x=>x!==tag):[...t,tag])} className={`rounded-full px-3 py-2 text-xs font-bold ${tags.includes(tag)?'bg-ink text-white':'border border-ink/10 bg-white text-ink/55'}`}>{tag}</button>)}</div></div><div className="mt-5 rounded-xl bg-mint/60 p-3 text-xs text-ink/65"><strong className="text-ink">Demo preview:</strong> this record stays local to the browser. Cloud storage and offline sync are Phase 2.</div><button onClick={()=>{onSaved();onClose()}} className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl bg-ink px-4 py-3 text-sm font-black text-white"><Check size={17}/> Save local record</button></div></div>}
