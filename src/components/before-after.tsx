'use client';
import { useState } from 'react';
export function BeforeAfter({before,after}:{before:string;after:string}){const [pos,setPos]=useState(50);return <div className="relative aspect-[16/9] overflow-hidden rounded-2xl bg-ink" onPointerMove={e=>{if(e.buttons===1){const r=e.currentTarget.getBoundingClientRect();setPos(Math.max(0,Math.min(100,((e.clientX-r.left)/r.width)*100)))}}}><img src={after} alt="After works" className="absolute inset-0 h-full w-full object-cover"/><div className="absolute inset-y-0 left-0 overflow-hidden" style={{width:`${pos}%`}}><img src={before} alt="Before works" className="h-full max-w-none object-cover" style={{width:'100%'}}/></div><div className="absolute inset-y-0 w-1 bg-white shadow" style={{left:`${pos}%`}}><div className="absolute left-1/2 top-1/2 grid h-8 w-8 -translate-x-1/2 -translate-y-1/2 place-items-center rounded-full bg-white text-xs font-black text-ink">↔</div></div><span className="absolute left-3 top-3 rounded-md bg-ink/70 px-2 py-1 text-[10px] font-black text-white">BEFORE</span><span className="absolute right-3 top-3 rounded-md bg-ink/70 px-2 py-1 text-[10px] font-black text-white">AFTER</span></div>}
