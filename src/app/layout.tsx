import './globals.css';
export const metadata = { title:'SiteSnap · Construction evidence', description:'Professional site photo records for small construction teams.' };
export default function RootLayout({children}:{children:React.ReactNode}) { return <html lang="en-GB"><body>{children}</body></html>; }
